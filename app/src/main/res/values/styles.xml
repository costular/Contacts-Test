<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorIcon">@color/colorIcon</item>
        <item name="primaryText">@color/colorPrimaryText</item>
        <item name="secondaryText">@color/colorSecondaryText</item>
        <item name="colorToolbar">@color/colorToolbar</item>
    </style>

    <style name="MyToolbar">
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark</item>
        <item name="android:background">?colorPrimary</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat" />

    <style name="ContactName" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">?primaryText</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ContactFavorite">
        <item name="srcCompat">@drawable/ic_favorite_contact</item>
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:tint">@color/colorContactStarred</item>
    </style>

    <style name="ContactDetailName" parent="TextAppearance.MaterialComponents.Headline5">
        <item name="android:textColor">?primaryText</item>

    </style>

</resources>
